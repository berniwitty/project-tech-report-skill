---
name: project-tech-report
description: 按固定模板把一个已完成的大模型/机器学习项目写成「项目技术报告」——阅读前须知、任务是什么、分阶段技术报告、基础知识、面试问题（思路 + 参考答案）、简历示例、术语表，一次交付完整成品，写给零基础读者。当用户说「项目技术报告」「写技术报告」「项目报告」「最终报告」「按模板重写报告」「项目学习报告」「面试前突击资料」，或给出项目文档、实验记录、简历段落并要求整理成兼顾学习、面试与简历的报告时，使用本 skill。与 llm-interview-notes 的区别：那个 skill 只产出简历驱动的背诵问答；本 skill 产出叙事完整的技术报告，任务定义、理论与简历都是正文的一部分，面试问答只是其中一节。
---

# 项目技术报告写作

目标：把用户做过的项目写成**一份结构固定、内容完整的技术报告**。读者设定是**零基础**——没接触过这个项目、也不熟悉相关方法的人，读完能讲清楚项目做了什么、每个数字怎么来的、被追问时怎么答。产出是成品，不是待填框架。

## 第 0 步：集齐四样输入，缺项先问

1. **立项文档**：实施规格、预注册、目标与非目标清单。**报告的"项目目的"必须来自这里**，不能从模板的框架反推，也不能把后期才发生的转向写成立项目的。
2. **事实源**：实验记录、结果表、配置文件、代码。每个数字与机制都要能落到这些源上；拿不准的去查，查不到的不写。
3. **简历段落**：决定面试问题一节的取舍，也是数字对齐的基准（见"与简历对齐"）。
4. **外部基准或论文**（如有）：只用于两处，见"外部基准的使用边界"。

## 三条贯穿全文的原则

### 原则一：目标是任务本身，不是超过谁

报告开头回答的是"**这个任务是什么、为什么难、我要把哪个量提上去**"，不是"我要超过某篇论文的基准"。跑分对照是结果的一部分，不是立项目的。

写"项目目的"时必须做三件事：

- 先用两三句话讲**任务本身**：用户要什么、系统要做什么动作、怎么算成功、基线是多少。
- 按立项文档列目标，并区分**立项目标**与**过程中产生的结论**。
- 单列一段 **"立项时的非目标"**：明确写了不做什么、为什么；如果后来做了，说明是什么触发的转向。这一段是面试高频问题的答案来源。

### 原则二：外部基准的使用边界

外部论文或榜单在报告里只出现两处：

1. **校准评测口径**：证明"我的评测器和别人量的是同一把尺子"，包括复现差距的排查与归因。
2. **结果对照**：正文接近结尾处一个小节，一张表列全场景。

其余章节不穿插"论文说…""与论文一致"这类引用。把论文的观察转写成自己的话，或者删掉。

**对照表必须列全，包括落后的格子**，并把三条前提一起说：训练数据是否相同、训练方式是否相同（例如对方按场景分别训练）、样本量是否相同。落后的格子要给出原因。藏短板在面试里是失分项。

### 原则三：零基础可读

- **第 1 节先讲任务**：一个具体例子（一段真实交互），动作空间，判分规则。
- **指标用同一个例子对比**：给一个"部分正确"的案例，算出宽松口径、严格口径、成功率各是多少，让读者看懂差异。
- **术语第一次出现就地解释**，文末给术语表。
- **第二部分开头先理顺名词**（基座、后训练、轨迹、采样、教师学生、三类后训练的区别），再讲具体方法。

## 全文结构（固定顺序）

```
标题
阅读前须知（提示框）
摘要：项目目的 / 主要结论 / 技术栈
第一部分：项目技术报告
    1. 任务是什么，为什么难
    2. 评测口径：先证明尺子是准的
    3..N. 阶段 k：<阶段名>
    N+1. 综合分析（各阶段贡献 + 与外部基准的对照）
第二部分：面试会追到的基础知识
    0. 先把这条链路上的名词理顺
    1..M. 每个方法一节
第三部分：面试问题
简历段落与使用方式
附：术语表
```

标题层级：顶层区域一级标题，编号章节二级，小节三级，小节内问题四级（Notion 只支持四级，不再往下分）。提示框用引用块；表格用 Markdown 表格；流程图用等宽字符画在代码块里，且只在确实帮助理解时才画。

## 每个区域的定义与要求

### 阅读前须知（提示框）

三项，不要更多：

1. **这份文档写给谁**：读者设定与读完能做什么。
2. **数字口径**：全文用哪个指标，各种样本量标注怎么读，出处在哪几个文件。
3. **项目一句话**：任务 + 起点数字 + 终点数字。

文档版本号、章节完成状态这类信息只在文档确实是半成品时写，否则删掉。

### 摘要

- **项目目的**：按原则一写。
- **主要结论**：编号列表，每条"**加粗结论句**：一到两句依据"。负结果、成本结论、被证伪的假设同样算结论，且要单独成条。简历上出现的每个结论都要在这里有对应条目。
- **技术栈**：用表格，三列——环节、用到什么、在本项目里做什么。**不写文件路径**。

### 第一部分

**1. 任务是什么，为什么难**：具体例子 → 判分规则 → 指标定义（同一个例子算三种口径）→ 场景表 → 基线输在哪（用漏斗或等价的分解，把总指标拆成几段的乘积）。

**2. 评测口径**：评测器与参考实现一致性怎么证明；复现差距的排查表（排查项 / 做法 / 结果）与归因；按什么规则决定不换主线；说明这个差距为什么不影响内部结论。

**阶段章节**：每个阶段固定几类小节，按需增减：

- **X.1 这一步在做什么**：先用一句话讲清这一步的方法叫什么、解决什么；再写起点状态与要解决的几类问题。
- **X.2 做法**：不按时间流水账，按"问题 → 做法 → 例子 → 结果数字 → 归因边界"组织。结果数字写分母。规则类内容用表格列出"规则 / 为什么有它"，并标出后来被推翻的那条。
- **X.3 对照实验**：固定四块——**已有结果**（一句加粗结论 + 对照表）、**理论分析**（区分"可能机制"与"已证实原因"）、**后续方案**（标"尚待验证"）、**结论**（当前采用什么、不排除什么）。
- **X.N 阶段结论**（提示框）：一句核心结果带起点终点分母；本阶段证明了什么；**必须主动说的边界**（哪些不是单变量对照、哪些不显著）；为什么带着这个结论进入下一阶段。

**综合分析**：一张表列各阶段的变化、增量、投入、结论类型；然后写贯穿全程的两三条方法线索；最后按投入产出排下一步。与外部基准的对照放在这一节的最后一个小节。

### 第二部分：面试会追到的基础知识

先一节"名词理顺"，再每个方法一节。每节固定四块：

1. **机制**：一句话定义 + 与相邻方法的区别。
2. **数据与目标函数**：样本长什么样；目标函数用文字讲清优化的是什么量；有公式就给公式，并配一个两三个元素的**手算例子**，算出结果。
3. **与本项目的联系**：用项目自己的数字解释理论预期与实际的关系。
4. **适用边界与误区**。

最后一节写"推理服务"或等价的工程节：哪些参数真的影响了实验结果。

### 第三部分：面试问题

每题一个二级标题，标题就是面试官的原话。每题两块：

- 💡 **思路**（提示框）：加粗一行 **回答主线：A → B → C → D**，说明面试官真正想听什么；末尾一行 📖 **依据**：指向正文章节。思路块要短，不重复参考答案的内容。
- 🗣 **参考答案**（提示框）：第一人称口语，三到五段，可直接说出来，1–3 分钟说完。

题目至少覆盖：项目介绍、数据怎么做的、核心方法为什么这样选、关键数字怎么确认是真的、负结果为什么、没做的路线为什么不做、复现差距怎么处理、最大不足与下一步、简历技术词逐词。**简历上出现的每个数字与技术词至少被一道题覆盖。**

### 简历段落与使用方式

开头提示框说明简历是现行版本、投递前按岗位取舍、不能把待验证写成已完成。然后两种用法（作为已有项目里的一条经历、独立成项），再给简历原文。

**简历原文一字不改地照录**，除非用户明确要求改简历。

最后一节 **"面试时要主动说清的 N 处边界"**：逐条列出简历措辞与实际做法之间需要补充说明的地方（口径差异、总量与实际用量的差异、术语歧义、已被推翻但简历仍保留的表述、对照的适用范围）。

### 附：术语表

两列表格，覆盖全文出现过的专有名词。

## 与简历对齐

这是硬要求，因为面试官拿着简历提问：

- 简历上的**每个数字**都要在报告里能找到出处与算式，且口径一致。
- 简历上的**每个技术词**都要在报告里有一段说明它在项目里做什么。
- 简历引用的**对照数字**（例如与某篇论文的比较）必须在报告的对照表里出现，不能因为"分场景后不好看"就删掉；正确做法是补全其余场景并说明前提。
- 报告里凡是与简历措辞不完全一致的地方，写进"要主动说清的边界"，不改简历。

## 写作规范

- **禁止比喻**；术语给机械级定义。
- **禁止模糊指代**：「它」「这个」「那样」离所指超过一句话就换成明确名词。
- **每段先说结论**，再给依据。
- **数字带分母与口径**；同一指标全文一个名字。
- **归因写限定**：一次改多个变量时写"一揽子改动"，把归因留给消融；不显著的差距写"持平""不显著"。
- **区分已完成与待验证**：后续方案标"尚待验证"；负结果如实写并给机制；**没测到数字的路线写"成本结论"而不是"负结果"**。
- **删掉不会被问的内容**：文件路径、脚本名、运维细节（磁盘、重启、守护进程）、文档管理规矩、无关的成本流水。判据一句话：面试官会不会开口问这个。成本数字只在它解释了某个决策时保留。
- 无 emoji（面试问题的 💡 🗣 📖 三个固定标记除外），无"希望对你有帮助"式结尾。

## 交付

1. 报告写成单个 Markdown 文件放在用户项目的 `docs/` 下（例如 `docs/TECH_REPORT.md`）。
2. 用文件发送工具把报告发给用户。
3. 若用户指定了 Notion 页面，把正文写进去，注意下面三条坑。
4. 回复里说明：改了什么、哪些数字来自哪些事实源、哪些标记为待验证、相对上一版的差异。
5. 用户要求修改时整篇重写、重新交付完整文件，不在旧文上打补丁。

### Notion 交付的三条坑

- **`update_content` 的字符串匹配经常失效**：Notion 会规范化引号等字符，长字符串匹配不上；多个替换是原子的，一个失败全部回滚。长文更新直接用 `replace_content` 整页重写。
- **一页写不下**：先 `replace_content` 写前半部分，再 `insert_content` 追加后半部分。
- **`replace_content` 会删掉子页面**：父页面有子页面时，必须在新内容里用 `<page url="...">` 标签把它们列回来，否则报错或丢失。

## 交付前检查清单

- [ ] 项目目的来自立项文档，且单列了"立项时的非目标"。
- [ ] 第 1 节有具体任务例子、判分规则、三个指标的同例对比、基线的分段分解。
- [ ] 外部基准只出现在"校准口径"与"结果对照"两处；对照表列全场景，落后的格子也在，三条前提一起说。
- [ ] 每个阶段有阶段结论提示框，含"必须主动说的边界"。
- [ ] 每个数字有分母或样本量；每个跨变量对比有归因限定。
- [ ] 基础知识部分有名词理顺节；每个方法有手算例子或公式。
- [ ] 面试问题覆盖简历的每个数字与技术词；每题有回答主线、依据、参考答案。
- [ ] 简历原文未被改动；"要主动说清的边界"逐条列出。
- [ ] 有术语表。
- [ ] 全文无比喻、无模糊指代、无文件路径与运维细节、无未标注来源的数字。

## 附件

- `templates/REPORT_TEMPLATE.md`：可直接填写的报告骨架。
- `references/example-report-excerpt.md`：范例报告原文提取，用于对照格式与文风。
