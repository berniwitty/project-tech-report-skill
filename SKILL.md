---
name: project-tech-report
description: 按固定模板把一个已完成的大模型/机器学习项目写成「项目技术报告」——阅读前须知、文档组织思路、摘要、分阶段技术报告、理论基础与应掌握知识、面试问题（思路 + 参考答案）、简历示例与使用方式，一次交付完整成品。当用户说「项目技术报告」「写技术报告」「项目报告」「最终报告」「按模板重写报告」「项目学习报告」「面试前突击资料」，或给出项目文档、实验记录、简历段落并要求整理成兼顾学习、面试与简历的报告时，使用本 skill。与 llm-interview-notes 的区别：那个 skill 只产出简历驱动的背诵问答；本 skill 产出叙事完整的技术报告，理论与简历是正文的一部分，面试问答只是其中一节。
---

# 项目技术报告写作

目标：把用户自己做过的项目写成**一份结构固定、内容完整的技术报告**，读者是准备面试的本人和看简历的面试官。报告同时承担四件事：讲清项目做了什么、用实验数据解释为什么、把用到的理论讲到能答追问、给出简历写法。产出是成品，不是待填框架。

## 第 0 步：先集齐三样输入

1. **项目事实源**：项目文档、实验记录、结果表、配置文件、代码仓库。报告里每个数字与机制都要能落到这些源上；拿不准的去查，查不到的不写。
2. **简历段落**（如有）：决定面试问题一节的取舍，也决定简历示例一节的起点。没有简历时，面试问题按报告主结论出题。
3. **已有的面试笔记或会话报告**（如有）：可以直接复用其中核实过的数字与机制，不重新推导。

## 全文结构（固定顺序，不增删顶层区域）

```
标题
阅读前请注意（提示框）
文档组织思路
摘要
    项目目的
    主要结论
    技术栈
第一部分：项目技术报告
    1. 项目目标
    2. 总体技术方案
    3..N. 阶段 k：<阶段名>
    N+1. 各阶段的综合分析
第二部分：理论基础与应掌握知识
第三部分：面试问题
简历示例与项目使用方式
```

标题层级：顶层区域用一级标题（`#`），区域内的编号章节用二级（`## 1. 项目目标`），阶段内小节用三级（`### 3.1 阶段目标`），小节内的问题用四级（`#### 3.3.1 如何……？`）。Notion 只支持四级标题，不再往下分。提示框统一用引用块或 callout；表格用 Markdown 表格；流程图用等宽字符画在代码块里。

## 每个区域的定义、内容与要求

### 标题

格式：`面向 <场景> 的 <方法主线>：<项目名> 项目技术报告`。标题说明验证载体与方法，不写结果数字。

### 阅读前请注意（提示框）

定义：告诉读者这份文档是什么、不是什么。固定包含五项，每项一段或一行：
1. 文档用途与局限（提供学习、实验分析和求职表达的思路；哪些部分仍是初稿）。
2. 简历免责（简历示例是初版，不能直接投递）。
3. 配合阅读建议（配合哪些原始文档、代码或讲解）。
4. **文档版本**、**文档状态**（哪些章节已成稿、哪些待补）。
5. **项目定位**：一句话，说清验证载体与研究目标。

### 文档组织思路

定义：先给一行箭头链（`概述 → 整个流程 → 各阶段实验结果 → 理论基础 → 面试问题 → 简历示范`），再用编号列表逐项说明每个区域读什么、为什么放在这个位置。每项一到两句，动词明确（"先了解""串起""分别看""带着数据学"）。

### 摘要

三小节，标题固定：
- **项目目的**：先一句话说明"以什么场景作为验证载体，但最终目的不是什么而是什么"。然后分两层写编号目标：**实验与效果目标**（1、2）与**项目通用性与面试目标**（3、4）。每条目标是"加粗短句 + 冒号 + 一两句展开"。
- **主要结论**：编号列表，每条是"**加粗结论句**：一到两句依据"。结论只写报告正文能支撑的，负结果与成本结论同样算结论。
- **技术栈**：按用途分组列出，每个词后面一句它在本项目里做什么。

### 第一部分：项目技术报告

**1. 项目目标**：三段。第一段说要把什么换成什么、要达到什么；第二段说做这件事面临的两到三个直接问题；第三段用编号列出阶段划分，每个阶段一句"通过什么手段解决什么问题、产出什么"。末尾一句说明最终关注的是哪几个量之间的平衡。

**2. 总体技术方案**：一段文字串起全流程，然后一张等宽字符流程图（每个方框一个环节，阶段方框内列出该阶段的三到四个动作），最后一段说明后续章节按什么顺序组织、每个阶段回答什么问题。

**阶段章节（3..N）**：每个阶段固定五类小节，按需增减中间小节：
- **X.1 阶段目标**：先写"本阶段的目标不是……而是……"；再写起点状态与主要问题（带基线数字与错误类型）；再用编号列出本阶段要解决的几类问题。
- **X.2 评测**：评测集怎么来、规模多大、评什么、判定标准是什么、主集与独立集怎么分；说明结论的适用边界。
- **X.3 核心方法**：不按训练轮次流水账，改成围绕三到四个问题组织，每个问题一个四级标题（"如何……？"）。每个问题的第一段固定以 **本段要讲清楚：** 开头，一句话说明这一段回答什么。正文按"起点 → 做法 → 例子 → 结果数字 → 归因边界"展开。结果数字要写分母（40/51），归因要写限定（"不能将全部增益单独归因于……，需要……消融"）。最后一段用"综上，本节的主线是：……"收束，并列出参考文献。
- **X.4 对照实验**：标题写"X.4 <方法> 对照实验与进一步优化方向"。固定四块：**已有结果**（一句加粗结论 + 对照表）；**理论分析**（为什么会这样，区分"可能机制"与"已证实原因"）；**后续方案**（编号，每条说明验证方法与判据）；**结论**（加粗一句，说明当前采用什么、不排除什么）。
- **X.5 阶段结论**：第一块是提示框 **核心结果**（一句话带起点、终点、分母）；然后三段：本阶段证明了什么；哪些数字不是严格单变量对照、正式判断以哪一节为准；最终为什么选这个方案进入下一阶段（含被放弃的候选与理由，以及交给非训练手段兜底的剩余问题）。

**综合分析（N+1）**：把各阶段的数字放到一张表或一条时间线里，回答三个问题：每个阶段贡献了多少、哪些结论之间互相支撑、按投入产出应该先做什么。

### 第二部分：理论基础与应掌握知识

开头一个提示框写 **状态**（哪些主题已成稿）。每个主题一个二级或三级标题，标题格式 `X.Y <方法>的理论基础：解决什么问题，适合什么任务？`。每个主题开头一个提示框 **对应实践：第一部分 X.Y**，并声明"本节介绍通用原理，不把本项目的结果推广为算法本身有效或无效"。正文按固定问题展开：
1. 要解决什么问题（一句话定义，一句话与相邻方法的区分，一个例子）。
2. 训练数据或输入是什么样，模型如何学习（数据三元组或样本格式；目标函数用文字讲清优化的是什么量；有公式就给公式和一个两三个元素的手算例子）。
3. 与本项目实验结果的联系（用本项目的数字解释理论预期与实际的关系）。
4. 常见误区或适用边界。
最后一节写 **拓展提醒**：与项目相邻、面试可能问到但项目没做的主题，各一段。

### 第三部分：面试问题

每题一个二级标题，标题就是面试官的原话（"你的数据集是如何做的？"）。每题两块：
- 💡 **思路**（提示框）：第一段加粗写 **回答主线：A → B → C → D**，并说明面试官真正想听什么、要区分什么；然后 **第一步、第二步……** 各一段，每段带项目数字；末尾一行 📖 **项目依据**：列出正文章节或文档位置，并注明数字的适用边界。
- 🗣 **参考答案**（提示框）：第一人称口语，三到五段，可以直接说出来，1–3 分钟说完；不出现"如上所述"这类书面语。
题目至少覆盖：项目介绍、数据怎么做的、评测怎么设计、核心方法为什么这样选、负结果或没做的路线为什么、基座或方案怎么选、最大不足与下一步。简历上出现的每个数字与技术词至少被一道题覆盖。

### 简历示例与项目使用方式

开头提示框 **重要说明**：简历示例是初版、需要按岗位打磨、不能把规划写成成果。然后 **两种使用方式**：方式一，作为已有项目里的一条经历（给出一段可贴的短句）；方式二，独立成项（说明展开哪些环节）。最后 **简历初稿**：给出可贴的项目段落；若用户已有定稿简历，原文照录并标注"现行版本"。

## 写作规范

- **禁止比喻**；术语给机械级定义。
- **禁止模糊指代**：「它」「这个」「那样」离所指超过一句话就换成明确名词。
- **每段先说结论**，再给依据；小节第一段用"本段要讲清楚"或"回答主线"引导。
- **数字带分母与口径**：40/51、45.51%（1,459 题）、同评测口径；同一指标全文一个名字。
- **归因写限定**：一次改多个变量时写"一揽子改动"，把归因留给消融；不显著的差距写"持平""不显著"，不写"更好"。
- **区分已完成与待验证**：后续方案标"尚待验证"；负结果如实写并给机制；没测到数字的路线写"成本结论"而不是"负结果"。
- 措辞与用户简历对齐：组名、指标名、实验名用简历原话。
- 无 emoji（面试问题的 💡 🗣 📖 三个固定标记除外），无"希望对你有帮助"式结尾。

## 交付

1. 报告写成单个 Markdown 文件放在用户项目的 `docs/` 下（例如 `docs/TECH_REPORT.md`），标题层级按上文规范，Notion 粘贴后能直接成为标题块。
2. 用文件发送工具把报告发给用户；若用户指定了 Notion 页面，用 Notion 工具把正文写入该页面。
3. 回复里说明：报告结构、哪些章节的数字来自哪些事实源、哪些内容标记为待验证或截图缺失、相对上一版改了什么。
4. 用户要求修改时，整篇重写、重新交付完整文件。

## 交付前检查清单

- [ ] 顶层区域齐全且顺序正确；阅读前须知含版本、状态、项目定位。
- [ ] 每个阶段有阶段目标、评测、核心方法、对照实验（如有）、阶段结论；阶段结论有"核心结果"提示框。
- [ ] 核心方法的每个小节以"本段要讲清楚"开头，末尾有"综上"收束。
- [ ] 每个数字有分母或样本量；每个跨变量对比有归因限定。
- [ ] 理论部分每个主题有"对应实践"提示框与一个手算例子或公式。
- [ ] 面试问题每题有回答主线、分步思路、项目依据、参考答案；简历每个数字被覆盖。
- [ ] 简历一节有重要说明与两种使用方式。
- [ ] 全文无比喻、无模糊指代、无未标注来源的数字。

## 附件

- `templates/REPORT_TEMPLATE.md`：可直接填写的报告骨架，每个区域附带引导句。
- `references/example-report-excerpt.md`：范例报告《面向 Agent 的本地小模型结构化输出后训练与部署》的原文提取，用于对照格式与文风。
