# 面向 <场景> 的 <方法主线>：<项目名> 项目技术报告

> **这份文档写给谁**：给没接触过这个项目的人，读完能讲清楚项目做了什么、每个数字怎么来的、被追问时怎么答。术语第一次出现就地解释，附录有术语表。
>
> **数字口径**：全文成绩都是 <指标名，附一句定义>。标"<样本量 A>"的是 <说明>，标"<样本量 B>"的是 <说明>。出处是 <文件>。
>
> **项目一句话**：<任务> + <起点数字> + <终点数字>。

---

## 摘要

### 项目目的

**任务本身**：<用户要什么、系统做什么动作、怎么算成功>。<基线模型> 直接做，<场景一> 只有 <数字>，<场景二> 只有 <数字>。

**项目要回答的问题**：<不换什么、把哪个量提上去、要说清什么>。

具体拆成 <N> 条路和一条约束：

1. **<路线一>**：<做什么，为什么>。
2. **<路线二>**：<做什么，为什么>。
3. **约束**：<算力 / 时间 / 数据的硬约束，以及它如何影响选型>。

<立项文档与预注册的约定：写进了什么、冻结了什么、能不能改。>

**立项时的非目标**：<明确写了不做什么，理由是什么>。<后来如果做了，说明是什么触发的转向，并声明由此产生的结论不是立项目的>。

### 主要结论

1. **<结论一>**：<依据与数字>。
2. **<结论二>**：<依据与数字>。
3. **<负结果或成本结论>**：<机制>。

（简历上出现的每个结论，这里都要有对应条目。）

### 技术栈

| 环节 | 用到什么 | 在本项目里做什么 |
|---|---|---|
| <环节> | <工具 / 方法> | <一句话职责，不写文件路径> |

---

# 第一部分：项目技术报告

## 1. 任务是什么，为什么难

### 1.1 一个具体任务长什么样

<环境是什么。可用动作只有哪几类。>

> <一段真实交互：用户说什么、系统做什么、怎么收尾>

判分时 <比对哪几个维度>。

任务难在 <两到三点，每点一句>。

### 1.2 成功怎么判定：<N> 个指标

- **<宽松口径>**：<定义>。
- **<严格口径>**：<定义>。
- **<成功率>**：<定义>。**本项目所有 headline 数字都是这个**。

用同一个例子说清差别：<给一个"部分正确"的案例，算出三个口径各是多少>。**绝对值低是这个任务的常态。**

### 1.3 <场景 / 任务类型>

| 场景 | <维度一> | <维度二> |
|---|---|---|

<其他固定设定：步数上限、对手模型、温度等。>

### 1.4 基线输在哪一段

把总指标拆成几段的乘积：

| 段 | 数量 | 转化率 |
|---|---|---|

**怎么读这张表**：

- <哪一段是最大漏洞，用一个对照实验证明瓶颈在哪>。
- <哪一段几乎不丢分，说明什么>。
- <还有哪些不体现在漏斗里的毛病>。

## 2. 评测口径：先证明尺子是准的

**评测器一致性**：<怎么证明与参考实现一致；修了哪些会让分数不可比的问题>。

**基线复现的差距**：<别人报告多少、我复现多少>。排查顺序与结果：

| 排查项 | 做法 | 结果 |
|---|---|---|

<管线没问题，差距的归因是什么，两条正面证据。>

<按什么规则决定不换主线；为什么这个差距不影响内部结论。>

## 3. 阶段一：<名称>

### 3.1 这一步在做什么

<一句话讲清方法叫什么、解决什么。起点状态与要解决的几类问题。>

### 3.2 做法与结果

<按"问题 → 做法 → 例子 → 结果数字（带分母）→ 归因边界"组织。>

| <对照表> | | |
|---|---|---|

### 3.3 <对照实验 / 被推翻的假设>

**已有结果：<一句加粗结论>。**

<对照表>

**理论分析：<为什么会这样，区分可能机制与已证实原因>。**

**后续方案（尚待验证）**：<编号，每条给验证方法与判据>。

**结论**：<当前采用什么、不排除什么>。

> **阶段结论**：<一句核心结果，带起点、终点、分母>。<本阶段证明了什么>。<必须主动说的边界：哪些不是单变量对照、哪些不显著>。<为什么带着这个结论进入下一阶段>。

## 4. 阶段二：<名称>

（同上结构）

## N. 综合分析

### N.1 各阶段贡献了什么

| 阶段 | 变化 | 增量 | 投入 | 结论类型 |
|---|---|---|---|---|

<贯穿全程的两三条方法线索。按投入产出排下一步。>

### N.2 与 <外部基准> 的对照

<简历上写了这组对照，所以必须能答，要分场景说、把落后的格子也说出来。>

| 场景 | 本项目最好结果 | <对方方案一> | <对方方案二> |
|---|---|---|---|

**三条前提必须一起说**：<训练数据是否相同>；<训练方式是否相同>；<样本量是否相同>。

<落后的格子的原因。>

---

# 第二部分：面试会追到的基础知识

## 0. 先把这条链路上的名词理顺

- **<名词>**：<一句话>。
- **<三类方法的一句话区别>**。

## 1. <方法>：解决什么问题

**机制**：<一句话定义 + 与相邻方法的区别>。

**数据与目标函数**：<样本格式；目标函数优化的量；公式>。

**手算**：<两三个元素的例子，算出结果>。

**与本项目的联系**：<用项目数字解释理论预期与实际>。

**适用边界与误区**：<两条>。

## M. <工程节>：哪些参数影响了实验

- **<参数>**：<现象与数字>。

---

# 第三部分：面试问题

## 1. 简单介绍一下你的项目

> 💡 **思路**
>
> **回答主线：<A> → <B> → <C> → <D>。** <面试官真正想听什么>。
>
> 📖 **依据**：<正文章节>。

> 🗣 **参考答案**
>
> <第一人称口语，三到五段。>

## 2. <下一题：面试官的原话>

（同上结构）

---

# 简历段落与使用方式

> **说明**：下面是现行版本，投递前按岗位取舍篇幅。<负结果与待验证的边界>已按事实写入，不能把续跑候选写成已完成。

## 两种用法

**放进已有的 <X> 项目里当一条经历**：

> **<小标题>**：<一段可贴的短句>。

**独立成项**：<展开哪些环节>。

## 现行版本

<简历原文，一字不改。>

## 面试时要主动说清的 <N> 处边界

1. <口径差异>。
2. <总量与实际用量的差异>。
3. <术语歧义>。
4. <已被推翻但简历仍保留的表述>。
5. <对照的适用范围>。

---

# 附：术语表

| 术语 | 意思 |
|---|---|
